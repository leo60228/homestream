{
  "name": "homestream",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd src; trap 'kill $(jobs -p)' EXIT; parcel index.html *.wasm & sleep 1; firefox http://127.0.0.1:1234/index.html & wait",
    "build": "cd src; parcel build --public-url ./ -d ../dist index.html *.wasm",
    "build:urls": "node --experimental-json-modules ./urls.js",
    "deploy": "(cd dist && (git rm -rf . || true)) && npm run build && cd dist && git add -A && (git commit -m upd8 && git push || true)"
  },
  "author": "leo60228",
  "license": "GPL-2.0-only",
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "audio-feeder": "^0.5.0",
    "libmp3lame.js": "^1.0.1",
    "lodash": "^4.17.20"
  },
  "browserslist": [
    "last 1 Firefox version"
  ],
  "type": "module"
}
